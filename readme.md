
# Application Toggle for KDE

# Prerequisites

Install `kdotool`. This can be done via `cargo install kdotool` if you have cargo.

# Installation

Place the `app-toggle.sh` script in a reasonable location (`~/.local/bin` is
recommended,) and ensure it is accessable via `$PATH`.

